import React from "react";
import './contacts.css';
import { Element } from "react-scroll";

export const Contacts = () => {
    return (
        <Element name='contacts' className='contacts'>Contacts</Element>
    );
}
